% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bo_v04_helpers.R
\name{build_initial_history_from_warmstart}
\alias{build_initial_history_from_warmstart}
\title{Build initial_history tibble from a warmstart_from specification.}
\usage{
build_initial_history_from_warmstart(
  warmstart_from,
  bounds,
  objective,
  constraint_tbl,
  progress = TRUE
)
}
\arguments{
\item{warmstart_from}{List whose elements are one of:
(i) a one-row data frame with parameter columns matching `names(bounds)`,
(ii) a path to a `*_best_design.csv` file (long format with columns
  `parameter` and `value`),
(iii) a one-row tibble in the BATON best-design wide format
  (column `total_n` etc., with operating-characteristic columns also
  present),
(iv) a list with element `theta` (named list of parameter values).}

\item{bounds}{Named list of length-2 numeric vectors (parameter bounds).}

\item{objective}{Name of the objective metric (character).}

\item{constraint_tbl}{Constraint table from `parse_constraints()`.}

\item{progress}{Logical; if TRUE, emit a message about how many designs
were converted.}
}
\value{
A tibble suitable for use as `initial_history` in `bo_calibrate()`.
}
\description{
Build initial_history tibble from a warmstart_from specification.
}
\keyword{internal}
