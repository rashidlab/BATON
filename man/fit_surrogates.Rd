% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surrogates.R
\name{fit_surrogates}
\alias{fit_surrogates}
\title{Fit Gaussian process surrogates for operating characteristics}
\usage{
fit_surrogates(
  history,
  objective,
  constraint_tbl,
  covtype = "matern5_2",
  use_hetgp = TRUE,
  prev_surrogates = NULL,
  fit_seed = NULL
)
}
\arguments{
\item{history}{tibble with columns `unit_x` (list of numeric vectors),
`metrics` (list of named numeric vectors), and optional `variance`
(list of named numeric vectors with noise variances).}

\item{objective}{name of the objective metric.}

\item{constraint_tbl}{tibble produced by [parse_constraints()].}

\item{covtype}{covariance kernel used by DiceKriging (default `"matern5_2"`).}

\item{use_hetgp}{logical; if TRUE and hetGP package is available, use
heteroskedastic GP with replicated observations. Default TRUE.}

\item{prev_surrogates}{optional list of surrogates from previous iteration
for warm-starting hyperparameter optimization. If provided, uses previous
hyperparameters as initial values, reducing optimization time by 30-50\%.}

\item{fit_seed}{optional integer base seed for reproducible hyperparameter
fitting. When supplied, each metric's fit runs under a deterministic seed
(`fit_seed` + metric index), making results identical regardless of core
count when fits are parallelised via `options(BATON.cores)`. `NULL` (default)
leaves the RNG untouched.}
}
\value{
A named list of fitted surrogate models (GP objects).
}
\description{
Implements heteroskedastic GP modeling when variance information is available,
using either hetGP (preferred) or DiceKriging with known noise variance.
}
