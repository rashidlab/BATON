% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ablation.R
\name{ablation_multifidelity}
\alias{ablation_multifidelity}
\title{Multi-fidelity ablation study for Bayesian optimisation}
\usage{
ablation_multifidelity(
  sim_fun,
  bounds,
  objective,
  constraints,
  policies = list(low_only = c(low = 500), med_only = c(med = 2000), full = c(low = 200,
    med = 1000, high = 10000)),
  seeds = 1:20,
  bo_args = list(),
  integer_params = NULL,
  progress = TRUE,
  ...
)
}
\arguments{
\item{sim_fun}{simulator callback passed to [bo_calibrate()].}

\item{bounds}{parameter bounds.}

\item{objective}{objective metric name.}

\item{constraints}{constraint specification (see [bo_calibrate()]).}

\item{policies}{named list mapping policy name to a named numeric vector of
fidelity levels (passed to the `fidelity_levels` argument of
[bo_calibrate()]).}

\item{seeds}{integer vector of seeds used for each replicate.}

\item{bo_args}{list of arguments forwarded to [bo_calibrate()] (e.g.,
`n_init`, `q`, `budget`, `seeds`).}

\item{integer_params}{parameters constrained to integers.}

\item{progress}{logical; if `TRUE`, strategy-level progress messages are
emitted.}

\item{...}{additional arguments forwarded to `sim_fun`.}
}
\value{
Object of class `BATON_multifidelity` containing `runs` (per-policy
  records) and `summary` (policy-level averages).
}
\description{
Runs the calibration routine under several fidelity policies to quantify
simulation budgets and operating characteristic performance.
}
