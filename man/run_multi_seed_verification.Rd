% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bo_v04_helpers.R
\name{run_multi_seed_verification}
\alias{run_multi_seed_verification}
\title{Run multi-seed verification of a calibrated design.}
\usage{
run_multi_seed_verification(
  sim_fun,
  theta,
  seeds,
  n_rep,
  objective,
  constraint_tbl,
  progress = TRUE,
  ...
)
}
\arguments{
\item{sim_fun}{Simulator function (same signature as `bo_calibrate`).}

\item{theta}{Named list / vector of parameter values defining the design.}

\item{seeds}{Integer vector of seeds to evaluate at.}

\item{n_rep}{Number of replications per seed.}

\item{objective}{Name of the objective metric (character).}

\item{constraint_tbl}{Constraint table from `parse_constraints()`.}

\item{progress}{Logical; emit per-seed progress messages.}

\item{...}{Forwarded to `sim_fun`.}
}
\value{
A list with elements:
  \itemize{
    \item \code{per_seed}: data frame with one row per seed, columns for
      seed, the objective, each constraint metric, feasible flag,
      runtime_s.
    \item \code{summary}: list with mean/sd of objective and each
      constraint metric across seeds, plus strict_feas (1 if all seeds
      pass, 0 otherwise) and strict_feas_count (integer, number of
      feasible seeds).
  }
}
\description{
Re-evaluates a single design (`theta`) at multiple independent seeds at
high fidelity, returning per-seed operating-characteristic estimates and
summary statistics plus a strict-feasibility flag.
}
\keyword{internal}
