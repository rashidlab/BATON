% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bo_warmstart.R
\name{sequential_refinement}
\alias{sequential_refinement}
\title{Sequential BO Refinement}
\usage{
sequential_refinement(
  sim_fun,
  bounds,
  objective,
  constraints,
  coarse_budget = 100,
  refine_budget = 50,
  shrink_factor = 0.5,
  save_intermediate = TRUE,
  output_dir = "results/sequential",
  ...
)
}
\arguments{
\item{sim_fun}{Simulator function}

\item{bounds}{Initial (wide) bounds}

\item{objective}{Objective to minimize}

\item{constraints}{Constraint list}

\item{coarse_budget}{Budget for coarse phase}

\item{refine_budget}{Budget for refined phase}

\item{shrink_factor}{Bound shrink factor (default: 0.5)}

\item{save_intermediate}{Whether to save intermediate results}

\item{output_dir}{Directory for intermediate saves}

\item{...}{Additional args to bo_calibrate}
}
\value{
List with coarse_fit and refined_fit
}
\description{
Convenience function to run coarse -> refined BO in one call.
}
\examples{
\dontrun{
result <- sequential_refinement(
  sim_fun = wrapper,
  bounds = bounds_wide,
  objective = "EN",
  constraints = list(power = c("ge", 0.80), type1 = c("le", 0.10)),
  coarse_budget = 100,
  refine_budget = 50,
  save_intermediate = TRUE,
  output_dir = "results/sequential"
)

# Access results
coarse_best <- result$coarse_fit$history[result$coarse_fit$history$feasible, ][1, ]
refined_best <- result$refined_fit$history[result$refined_fit$history$feasible, ][1, ]
}
}
